
Simulação de Envio de Dados (Python – MQTT Publisher)

Arquivo: teste_publicador.py

import paho.mqtt.client as mqtt
import time
import random

broker = "broker.mqtt-dashboard.com"
topic = "conectball/velocidade"

client = mqtt.Client("SimuladorConectBall")
client.connect(broker, 1883, 60)

while True:
    velocidade = round(random.uniform(30, 120), 2)  # simula velocidade entre 30km/h e 120km/h
    client.publish(topic, str(velocidade))
    print(f"Velocidade enviada: {velocidade} km/h")
    time.sleep(2)
